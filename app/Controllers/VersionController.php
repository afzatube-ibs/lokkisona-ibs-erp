<?php

namespace App\Controllers;

use App\ActivityLog;

class VersionController extends Controller
{
    public function index()
    {
        $this->authorize('version.view');
        ActivityLog::record('version_access', 'Version page viewed');

        $this->render('version.index', [
            'pageTitle' => 'Version',
            'breadcrumbs' => [
                ['label' => 'System', 'active' => false],
                ['label' => 'Version', 'active' => true],
            ],
            'info' => $this->buildVersionInfo(),
        ]);
    }

    private function buildVersionInfo()
    {
        return [
            'product' => config('app.name'),
            'version' => config('app.version'),
            'codename' => config('app.release_label'),
            'release_date' => '2026-06-13',
            'production_launch_doc' => 'docs/PRODUCTION-LAUNCH.md',
            'php_version' => PHP_VERSION,
            'php_requirement' => 'PHP 8.2+',
            'environment' => config('app.env'),
            'dependencies' => [
                'OpenCart' => 'None',
                'WooCommerce' => 'None',
                'OCMOD' => 'None',
                'ZIP Installer' => 'None',
            ],
                        'features' => [
                'v2.5.3.2 Sync Products Section Polish: Product Import Preview tab — cleaner preview table with row readability (model/name/warehouse badge/variants), improved empty states, clearer import confirmation with product count and card footer — UI/copy only, no sync logic changes',
                'v2.5.3.1 Sync Mapping Section Polish: Status Mapping tab owner-friendly layout — two separate cards (New Order Import Mapping, Delivered / Returned Mapping) with card footers; softer mapping hint; removed In Queue column and debug checkbox (collapsed developer link); Import as New / Ignore labels — UI/copy only, no mapping logic changes',
                'v2.5.3 Sync Settings Release Polish: vertical sidebar hub with six panels (Connection, Status Mapping, Products, Order Sync, Maintenance, Recent Activity); Connection read-only summary with Edit Settings mode; sync-tools split into Order Sync / Maintenance / Recent Activity; copy renames (Product Import Preview, Entry/Final Mapping, Maintenance Tools); legacy ?tab=sync opens Order Sync; queue API status in connection summary — UI/copy only, no sync logic changes',
                'v2.5.2 Sync & Mapping UI Fixes: Products tab empty-state gating (no blank rows on load; correct field binding for model/name/warehouse/options); import checkbox/button disabled until valid preview; conditional queue-mapping alert scoped to Status Mapping and Products tabs; green Connection OK chip; mapping tab done badge when queue statuses loaded — no tab structure, reset cards, or sync history changes',
                'v2.5.1 Sync Settings UI Polish: tabbed Sync & Mapping hub (Connection, Status Mapping, Products, Sync & Tools); queue-only status table by default with debug show-all + search; no live OpenCart ping on page load; empty states until Refresh/Test; fixed horizontal import confirm row; mapping attention badge; reset cards and sync history (5 rows) — no mapping logic or Product Control changes',
                'v2.5.0 Sync & Mapping Settings: one owner page — Entry Status Mapping (Import as NEW / Ignore only), Final Result Mapping (Delivered / Returned after Dispatched), connection, product sync, order sync, reset tools, sync history; OpenCart creates NEW only; supplier owns Accepted/Packed/Dispatched; re-sync snapshot-only before Dispatched; forward-only final result after Dispatched; Status Mapping removed from primary nav — no Product Control, ledger, or connector logic changes',
                'v2.4.8 SFM Direction Repair: refocus IBS-LK into lean Supplier Fulfillment Manager — seven SFM status labels (New/Accepted/Packed/Dispatched/Delivered/Returned/Cancelled) on order workflow; sync limits enforced at 20 products and 20 orders per request with single-page product refresh and SyncRequestGuard one-at-a-time lock; re-sync never overwrites workflow status (courier forward promotion disabled); canSyncHub() unlocks Sync Settings for owner/admin; supplier portal scoped to order-assigned products only (no full catalog nav); order list shows OC ID, phone, address, invoice/COD amount; payable formula simplified to delivered cost minus returns minus advance minus settlement; reports trimmed to Inventory Snapshot and Sales/Order only; dispatch bridge warning uses supplier-products copy — no OpenCart writes, no DB migration',
                'v2.4.2 Order Workflow Safety: OpenCart sync stops at Shipped; status mapping imports only up to Shipped; IBS owns post-shipment workflow; dispatch report freezes order; pre-dispatch hub return path (Shipped → Delivery Stop → Hub Returning → Hub Return Confirmed) with no payable/ledger/return report; post-dispatch courier lane (Dispatched → In Review → In Transit → Out For Delivery → Delivered) forward-only via sync; customer return → Return Report after dispatch; backward movement and hub return blocked after dispatch; dispatch eligibility requires mapped product + cost > 0 + not hub return; manual sales orders (sync_source=manual) never overwritten by sync; demo orders hidden and blocked from dispatch — no ledger, no Product Control changes, no navigation redesign',
                'v2.4.1 Return Statement UAT Repair: Supplier Return Statement print layout (A4 landscape, fixed column widths, word wrap); screen table wrapping; Return Amount label (was Total Return Adjustment); client mixed-supplier guard alongside mixed-source; ReturnStatementLinePresenter reconciles display to locked ibs_return_report_items.product_cost_snapshot; handoff accounting chain Dispatch → Return → Payment → Balance; v2.5 ledger hook documented to use locked dispatch snapshot not catalog cost — no schema migration',
                'v2.4.0 Returns Foundation: Supplier Return Statement module (/return-reports, /return-report/{ref}, print) — one-step lock from supplier-confirmed Hub/Customer returns; duplicate/mixed supplier-source/missing cost-reason guards; ReturnStatementLinePresenter with Return Reason + Return Type columns; confirmReceive staging fix (no auto per-order batch); legacy Return Batch UI retired; migration 0007_return_reports_foundation.sql (manual apply) — no ledger posting, independent from Daily Dispatch',
                'v2.3.0 Daily Dispatch Supplier Statement Foundation: dedicated Daily Dispatch Statement print layout (/dispatch-report/{ref}/print) — not invoice wording; supplier-scoped eligible list with business-source filter and mixed-source block; one-step lock unchanged; transaction re-check; payable checkpoint PCP draft cross-link on detail (no ledger posting); return receive badges; Order List copy aligned to Daily Dispatch — no schema migration, no OpenCart writes, no stock deduction',
                'v2.1.8.3.2 POIP Option Image Mapping Repair: option rows resolve real variant images from POIP / Improved Options (API payload + read-only OpenCart DB enrichment); 3-tier display priority option → product → placeholder without forcing parent image on every row; simple/zero-variant products hide Option Rows; main product thumbnail no placeholder overlap — sync import and workspace read only',
                'v2.1.8.3 Product Control Adjustment UX Repair: compact Adjust Price / Adjust Quantity triggers (simple IBS Details + per option row); structured popover with Change Type, Method (price), Amount/Quantity, required reason dropdown (Forward to Wholesale, Manual Correction); server-side reason whitelist validation; simple/zero-variant products hide Option Rows; Inventory planning doc internal only — no launch UI block',
                'v2.1.8.2 Product Control Modal Completion Repair: IBS Details right panel (IBS Category, Main IBS Model, Low Warning, Status); Product Information left panel without IBS Category; Live Stock label; 64px product thumbnail with clean placeholder; option image 3-tier fallback (option → product → placeholder); option table ERP columns (IBS Model, Rate, Live Stock, IBS Stock); center-aligned table with sticky header; per-row Rate/IBS Stock adjust for variable products; simple-product Rate/IBS Stock in IBS Details only; history field labels Rate/IBS Stock — lazy local ERP only, no commit to OpenCart',
                'v2.1.8.1 IBS Category Visibility Repair: IBS Category column, filter, and Product Information editor always visible (no migration gate); clean "IBS Category: value" label in modal; dropdown + add-new edit; saves to ibs_products.supplier_product_category; preserved on re-sync',
                'v2.1.8 IBS Category Enhancement: ERP-local IBS Category column on Product Control list (center-aligned, filterable, sortable); Product Information modal field with double-click dropdown editor (main product only, options inherit parent); suggested categories with create-if-missing and soft duplicate-name normalization; category preserved on product re-sync — never written to OpenCart',
                'v2.1.7.1 Product Control Workspace Repair: status filter dropdown replaces chip row; compact single-row filters (Search, IBS Model, Category, Type, Sort, Page Size, Status Filter); ~55px KPI strip; list table columns with center alignment except Model No; wider Model No column; health badge colors; product count subtitle; 56–60px rows with 44×44 thumbnails; modal 60/40 snapshot/supplier split; option rows dominate variable modal; double-click edit, dirty save gating, cost/stock adjustments with required reason; history only in Cost/Stock History tab — lazy local ERP load only, no new modules',
                'v2.1.7 Product Control Center UX Completion: 60/40 Product Snapshot + Supplier Control top row with equal height; read-only snapshot labels (Product ID, Main Model, Stock); supplier status display-only; option rows table with 44×44 thumbnails and product image fallback; double-click edit with dirty row highlight and Save All Changes gating; cost/stock adjustment modal (+/− fixed, +/− percent, increase/decrease) with required reason and negative stock prevention; automatic health; history confined to Cost/Stock History tab with split Old/New columns; lazy local ERP load only — no OpenCart API on modal open',
                'v2.1.6 Navigation Organization Foundation: sidebar regrouped into Operations, Supplier Management (Products label), Reports, System (Settings + Version), Admin Tools, and Future Plans; all sections expanded by default except Future Plans; no route or permission changes',
                'v2.1.5 Login Page Final Dark SaaS Centered Design: single centered dark glass login card with bottom-corner glow/mesh, IBS branding, icon inputs, password toggle, Remember me (visual only), trust badge and page footer — no split layout, no KPI/demo cards; UI only, no auth/route changes',
                'v2.1.4 Modern SaaS Login Page Revamp: split hero + login card layout, supplier portal branding, password visibility toggle, clean error styling, mobile-first responsive login — UI only, no auth/route changes',
                'v2.1.3 Navigation Organization Foundation: sidebar regrouped into Operations, Supplier Management (Products label), Reports, System (Settings + Version), Admin Tools, and Future Plans; all sections expanded by default except Future Plans; no route or permission changes',
                'v2.1.0 Navigation Architecture Rebuild Foundation: modern SaaS sidebar with Dashboard, collapsible Operations/Finance/Settings tiers, single Reports hub, System Settings nested submenu (Users, Roles, Suppliers, Business Sources, Database Safety), Future Modules collapsed by default for planning/dev pages; compact spacing, section icons, tier expand state in localStorage — no route or permission changes',
                'v2.0.2.2 Product Control Center Compact Layout Fix: snapshot card height auto (no empty stretch); supplier strip top-aligned with inline Label: value fields; variable products hide product-level cost/stock/adjust (option rows only); history table confined to Cost/Stock History tab; simple products keep compact layout without middle whitespace — no DB changes',
                'v2.0.2 Product Control Center ERP Workspace Rebuild Foundation: 70/30 Product Snapshot (read-only, larger image, total stock/variants/health) + Supplier Control card (Iqbal & Brothers read-only, dblclick edit fields, cost/stock quick +/- buttons with note modal); full-width Option Lines table (OC Stock, history clock icon, compact ERP rows, real option images); sticky Save All Changes footer; existing save/history/data logic preserved — no DB changes',
                'v2.0.1 Product Control Center Final Supplier Editing UX: read-only OpenCart Snapshot (left) vs editable ERP Supplier Control (right); Iqbal & Brothers (IBS) supplier name display-only; compact modal header with OC ID, Main Model, health badges; Supplier Cost / Stock / Vendor Stock labels; variable products manage cost/stock from option rows (dblclick inline edit, right-click adjust); simple product cost/stock adjust popover with note; Low Stock / Out of Stock / Healthy from Vendor Stock vs Low Warning; enriched history (field, change type, delta, user); Save/Cancel dirty state; option image fallback; local ERP only — no OpenCart writes',
                'v2.0.0 Dispatch Batch Management & Cost Snapshot Foundation: clean /dispatch-reports batch list (reference, supplier, created by, qty, cost snapshot, view/print); enriched /dispatch-report/{batch} detail with product lines, business source, courier/consignment/OC status; missing-cost block on dispatch create; [action:create_dispatch_report] workflow history; VF lock note with batch ref; A4 print layout; payable draft notice (non-blocking); menu renamed Dispatch Reports — no sync/mapping/OpenCart writes/stock deduction',
                'v1.9.9 Dispatch Report Operational Foundation: removed VF debug UI from release; compact workflow header cards; Created Report row lock with View Report, Delivery Stop, Hub Return and "Included in Dispatch Batch" note; read-only /dispatch-report/{batch} view with Print and Export PDF; Created Report card shows latest batch reference; recent workflow history (20 rows); improved empty filter state — v1.9.8.1 filter consistency preserved, no sync/mapping/import changes',
                'v1.9.8 Vendor Fulfillment Release Completion: All status card filter with matching counts; per-row timeline button; enriched workflow history (action, batch, user); Created Report lock guard; bulk mixed-status warning; debug blocks hidden unless ?debug=1; release-quality vendor fulfillment ready for owner testing — no OpenCart writes',
                'v1.9.7 Vendor Fulfillment Workflow Completion Foundation: Packaging → Shipped no longer disappears — post-action redirect lands on the new status filter with success link; legacy ibs_status aliases (processing, ready_for_dispatch) match list/count filters; Hold resume always restores Order Received; bulk Resume on Hold filter; Created Report primary View Report button; shipped rows visible without batch/courier; re-sync never resets ibs_status; local ERP snapshot only — no OpenCart writes',
                'v1.9.4 Navigation Release Structure Cleanup: sidebar regrouped into Dashboard + Fulfillment (Order, Manual Order, Dispatched Total, Return List, Payables, Product Control) + Finance (Balance Sheet, Payable & Settlement, Opening Balance, Supplier Account / Ledger) + Reports tier + Settings + Future Modules; Sync Settings shows saved URL, masked token hint, connection/API/bridge status, last test and sync timestamps; buttons Save API Settings, Test Connection, Refresh Products, Sync Log — no route deletion, no workflow logic change',
                'v1.9.6 Order Sync Mapping Safety Foundation: status mapping seeds IBS initial fulfillment at import only — existing orders refresh OpenCart snapshot fields without resetting ibs_status; duplicate prevention by business_source_id + source_order_id; preview counts fetched/eligible/updated_snapshot/skipped; migration 0015 draft for origin status columns and unique keys — manual apply only',
                'v1.9.5 Vendor Fulfillment Workflow Release Foundation: /order-workflow operational release for first real testing — New Order → Receive → Order Received → Start Packaging → Packaging → Mark Shipped → Shipped → Create Dispatch Report → Created Report (locked View Report only); release status cards (5 main + Delivery Stop / Hub Return / Customer Return chips); row + bulk actions with Receive Order, Print & Start Packaging, Mark as Shipped, Create Dispatch Report modals; DDMMYYYY-P1/P2 batch reference; immutable dispatch snapshot; payable draft hook with warning when ledger table missing; audit log on every action; default 20 / max 50 rows; no OpenCart writes or page-load API — no new workflow stages or future modules',
                'v1.9.3 Vendor Fulfillment IBS-LK Parity: /order-workflow aligned to old IBS-LK extension — compact status cards + exception chips, unified toolbar (search, single status-aware bulk action, Hold/Cancel, per-page 20/50, Clear, owner Test Sync link); founder modals (Print & Move to Processing, Dispatch Report Created, Confirm Hub Return Receive); 3-dot menu (View timeline, Add note, Hold, Cancel, Delivery Stop); Created Report View Report + locked message; GET /order-workflow/history + POST /order-workflow/note + selection-preview JSON; homogeneous bulk validation server-side; green success toast — no OpenCart writes',
                'v1.9.2 Vendor Fulfillment Workflow Foundation: /order-workflow becomes compact Vendor Fulfillment SaaS table (cost-only product cards, SQL pagination 20–50 rows, filters, bulk Receive/Packaging/Shipped/Dispatch Batch); OrderWorkflowListReadService + row presenter; lazy action modal via order-workflow.js; IBS workflow independent from OpenCart OC status badge (read-only); dispatch batch from table when migration 0006 ready; no OpenCart writes — order sync remains on hidden /sync-preview',
                'v1.9.1 Product Control Performance: SQL-paginated listing (20 rows per page from local ERP snapshot only — no 200/500 row preload); KPI summary via aggregate SQL with 60s file cache; lazy AJAX modal via GET /product-control/workspace and GET /product-control/history; snapshot freshness strip with 24h stale warning; no OpenCart API on page load; dev-only timing diagnostics; migration 0013 optional list indexes — sync import and supplier field preservation unchanged',
                'v1.9.0 Supplier Intelligence Dashboard Revamp: /dashboard becomes read-only IQBAL & BROTHERS (IBS) Supplier Intelligence & Fulfillment Command Center — compact IBS header (no logo image), insight strip, 8 KPI cards with sparklines, weighted IBS Performance Score, Dispatch SLA from Order Received → Created Report (12h target, not courier delivery), payable command center, trend chart, top products/categories, catalog health, return intelligence, priority action cards; SupplierIntelligenceDashboardService composes existing analytics with safe empty fallbacks; owner and supplier both scoped to config auth.supplier_id (IBS) — no workflow writes, no migrations',
                'v1.8.7.4 Product Control Center Finalization: compact listing with OC model bold + OC ID (name searchable only); tight fixed-width table; 5 KPI filters; Catalog Health / Refresh Products / Sync Log header; modal hero split with compact OC facts + Iqbal & Brothers (IBS) supplier card in view/edit chip mode (Edit / double-click, Save All Changes); flat variant lines table (Line, Image, Model, Vendor Model, cost/stock, Warning, Health); supplier fields preserved on re-sync — no manual create, no product removal, no OpenCart write',
                'v1.8.7.3 Product Control Old IBS UI Match Repair: list page restored to read-only Inventory Products table (parent rows only, row-click popup); hero card + 4 KPIs with subtexts; Search & Filter card with type/sort and Order Received chips; Product Control Center modal with Main Product + Vendor Mapping cards and flat variant lines table; Sync Products header POST refresh from Lokkisona API (from_warehouse=1); preview/import/testing UI removed from Product Control (Sync Preview only); supplier fields preserved on re-sync — no OpenCart write, no migration',
                'v1.8.7.1 Product Control Working Center Finalize: compact sync strip with single checkbox-confirmed import; preview collapsed when catalog populated; migration 0012 notice collapsed; 6-col clickable KPI row; toolbar filters with scrollable chip groups; product rows with platform RO dl zone + ERP editable zone + footer save; variant compact table in details; fixed empty states and pagination placement — no sync logic, route, or migration changes',
                'v1.8.7 Product Control Clean Working Center: inline supplier catalog cards with summary KPIs (synced products, variants, missing cost/model, low stock, synced today); compact sync action bar (preview, import, reset, last sync, source label, from_warehouse badge); simplified search + filter chips including active/inactive; per-product Save ERP Fields with read-only OC fields and expandable variant rows; health badges Complete/Needs Cost/Needs Model/Low Stock/Needs Work; image_path/option_image_path thumbnails with placeholder; planning blocks removed from daily view — no order/dispatch/return/payable/OpenCart write/migration changes',
                'v1.8.6 Navigation Reorganization: sidebar regrouped into Fulfillment, Finance, Reports, Settings, Tools, System, and Developer tiers; Sync Preview and Status Mapping removed from daily menu; product sync/import/reset embedded in Product Control; order sync stays on hidden /sync-preview route; Pre-acquisition placeholder; no route deletion, no sync logic change, no migration',
                'v1.8.5 Supplier Product Control Completion: strict from_warehouse=1 supplier filter on OpenCart read with skip counts; preview/import and Product Control show supplier-synced products only with "Supplier synced only" badge; ERP-only Reset Product Sync Data (synced products, variants, preview session — not orders/payables/API settings); re-import preserves supplier fields; filters/chips/thumbnails/workspace edit; sync-api-settings uses connection_test only on page load — no order/dispatch/return/payable changes',
                'v1.8.4 Sync Performance and Real API Product Integration Repair: Test Connection uses api/ibs/connection_test (success + read_only, no bridge gate); product preview trusts real api/ibs/products rows with from_warehouse=1; bridge errors only when API reports failure with bridge/dispatch message; single API call per preview load; no product fetch on status page load — no order/dispatch/return/payable changes',
                'v1.8.3 OpenCart Read-Only API Foundation: OpenCart-side package (packages/opencart-ibs-read-api-v1.8.3) with read-only routes api/ibs/connection_test, api/ibs/products, api/ibs/orders — Dispatch Location bridge filter, max 20 rows, API token auth, JSON only; ERP docs and staging config examples for api/ibs routes — no ERP workflow, dispatch, return, or payable changes',
                'v1.8.2.2 Sync API Settings SaaS UI Polish: ops safety strip, KPI status row, 2-column status + form layout, form-input styling, header actions to Sync Preview and Product Control, compact security footer, Setup Guide collapsed — sync logic and config file storage unchanged',
                'v1.8.2.1 Sync API Settings Release UI Repair: production-style Sync/API Settings page — mode badge (Demo/Staging/Live/Error), visual connection summary, 2-column form, security note, setup guide, Reset to Demo; cleaner Product Sync Help table on Sync Preview — logic unchanged, config file storage only',
                'v1.8.2 Sync API Settings Foundation: System → Sync/API Settings page (Demo/Staging/Live source mode, Source URL, hidden API key save to gitignored config/opencart.local.php, product/order routes, read-only lock, sync toggles, bridge required, Test Connection); product sync diagnostics help block on Sync Preview with fix guidance for connection, bridge, from_warehouse, API route, options, ERP tables — no OpenCart write, no DB migration, no hardcoded staging URL in code',
                'v1.8.1 Product Sync Import Repair: product-only WriteGate (ibs_products, ibs_product_variants, ibs_business_sources) decoupled from order sync tables; preview session stores bridge-filtered import rows (from_warehouse=1 only); import capped at 20 with clear result message (Products imported / Variants imported / Skipped); re-import updates platform fields only — supplier model, cost, stock, note, and active/inactive never overwritten; Product Control reads same ibs_products tables sync writes to — no OpenCart write, no order import changes',
                'v1.8.0 Product Control Center Complete Foundation: paginated catalog (20/page) with GET filters and health badges (Ready, Needs Work, Low Stock, Missing Cost, Missing Supplier Model, Sync Required); thumbnail column; Product Control Center modal with sync info, completeness badges, read-only OC fields, collapsible variant accordion with option images; re-sync protection unchanged — no manual create, no OpenCart write, no migration',
                'v1.7.1 Live Product + Order Sync Connection Test: paginated read-only product/order preview (20/page, Previous/Next); two-step preview → owner confirm → import; Dispatch Location bridge filter (from_warehouse=1 only, no fallback); supplier-handled order filter via status mapping; courier/consignment on import; option price/model/stock read-only in preview; POIP/Related Options detection documented — no OpenCart writes, no migration, no dispatch/payable/stock/invoice changes',
                'v1.7.0 Product List/Edit Completion + Lokkisona Product Sync: warehouse pull syncs parent + option/variant lines (read-only from OpenCart, max 50); Product Control list columns (name, OC ID, category, last synced); modal edit for status, supplier assignment, supplier note (migration 0012 column gate); "No option synced" for variable products without options; cost/stock history on workspace save; Sync Preview Product Sync Status card — no manual product create, no OpenCart writes, no order/payable/dispatch/return changes',
                'v1.6.2 Modern SaaS UI Phase 3: finance and admin page polish, modern login screen, status-mapping and setup-tier alignment — visual only; no business-logic, schema, or route changes',
                'v1.4.0 Unified Business KPI Dashboard: one Plecto-style wallboard for owner and supplier — sales MTD gauge, orders ratio, top products/categories, 6-week sales trend, avg sale/order, return rate, pipeline, payments, catalog; owner sees retail BDT columns, supplier sees sale amounts only; ERP supplier_product_category on products (migration 0011) + Product Control field + Sales by Category report',
                'v1.3.1 Supplier Dashboard Analytics: professional supplier overview — 4 hero KPIs, order pipeline bar chart, 6-month growth columns (orders + offline sales), payment/ledger trend, product catalog snapshot; removes duplicate workflow task grid from dashboard; Orders page labeled as operations work queue',
                'v1.3.0 Supplier Full Business App: supplier role becomes a full scoped business app — SupplierContext binds supplier_id on login; expanded permissions for payables (manual debit/credit drafts), reports, settlements (read), dispatch (read), products, manual orders; modern supplier dashboard KPI grid; server-side supplier_id scoping on finance/ops modules; calculator keyboard + quick invoice date save + WriteGate/reprint UX fixes — owner still posts dispatch payables and settlements',
                'v1.2.1 Dashboard Metrics Fix: accurate COUNT(*) for dashboard and supplier task KPIs — replaces count(findByStatus LIMIT 50) that silently capped Active Orders, Shipped Awaiting Dispatch, workflow breakdown, and supplier task grid at 50; adds OrderWriteRepository::countByStatus, countByStatuses, countReturnPending — read-only metrics only, no UI or schema changes',
                'v1.2.0 Ops UI Alignment: extends v1.1.0 design system to daily ops pages — shared ops-safety-strip partial, kpi-grid replaces stats-grid on payables/settlements/sync-preview, compact page headers, workflow stage card accent borders, setup-tier pages (users, roles, suppliers, business-sources, opening-balances) collapse planning/reference into planning-collapsible — visual only, no business-logic or schema changes',
                'v1.1.0 UI/UX Revamp: NexaERP-inspired owner command center — tiered sidebar (Primary Fulfillment/Finance/Catalog ~12 links, collapsed Setup & Admin / System / Developer tiers), env-gated DEV banner (local/staging only), dashboard welcome strip + 6 KPI cards + workflow breakdown + needs-attention widgets + recent activity; supplier dashboard keeps 4-link nav with matching KPI card styling; full dark/light theme via CSS tokens — no business-logic, schema, or route changes',
                'v1.0.0 Production Launch: first production-ready release tag — full supplier fulfillment ERP for Iqbal & Brothers (order workflow, dispatch snapshots, payables, return batch deductions, settlements, reports, controlled OpenCart Test Sync); follow docs/PRODUCTION-LAUNCH.md on the server for credentials, migrations, opening balance cutover, and owner-approved live import (1–3 orders first); stock deduction and live full sync remain blocked until separate owner builds',
                'v0.8.0 Release Candidate Hardening: stability + consistency pass — all 9 operations page subtitles now show "v<version> — <release label>", settlements form actions use the url() helper (no hardcoded paths), every one of the 69 routes verified to resolve to a real controller method, all controllers lint-clean (no duplicate methods, balanced braces — SupplierPayablesController::netPayableFormula confirmed intact), supplier role confirmed scoped away from payables/settlements/sync/migrations, no mojibake; no new schema, no new features',
                'v0.7.3 Operations UI Completion: refactored dispatch-reports, return-receive, supplier-payables, sync-preview, status-mapping, invoice-printing to the gold-standard layout — compact safety strips replacing Safety Badges cards, primary actions first, all developer/planning content in collapsed planning-collapsible; removed the obsolete "Future Return Batch Plan" card (batching is live); added return-batch detail rows showing each return line; added OC Order Status to workflow cards; dynamic version on manual-orders/order-workflow subtitles; dashboard collapsible standardized',
                'v0.7.2 Return Batch & Payable Deduction: owner creates Return / Damage Deduction draft from an owner-approved return batch on Supplier Payables (POST /supplier-payables/post-return-batch) — never auto-deducted, draft still requires separate owner approval before posting',
                'v0.7.1 Workflow Card Columns & Return Batch Page: order cards show Total Qty, Total Cost (snapshot), Source / OC Ref and per-line cost; /return-receive gains Create Return Batch (R+DDMMYYYY reference, max 50, same-supplier) feeding the owner approve flow',
                'v0.7.0 Operations UI Sweep: all operations pages show live app version (no stale v0.4.x/v0.5.x labels), standardized planning-collapsible reference blocks, settlements summary stat cards + empty state, product-control encoding fix — no business-logic or schema changes',
                'v0.6.3 Order Workflow Create Modal: /order-workflow status filter pills, Recent Workflow History table, in-page Create New Order modal (POST /order-workflow/create) reusing manual order multi-line form',
                'v0.6.2 Manual Order UX: /manual-orders dropdowns for source/supplier/product, multi-line entry with live totals and cost snapshot display, Recent Manual Orders card, developer content collapsed',
                'v0.6.1 Supplier Engagement Tools: header calculator + quick invoice modals, multi-line invoice with discount/advance, professional print layout, DB persistence (0007+0010)',
                'v0.6.0 Production Launch Readiness: staging QA checklist, production cutover doc, credential hardening guidance — owner sign-off required before live',
                'v0.5.9 Full Settlement Workflow: /settlements period prepare, owner approve, mark paid, close — migration 0009',
                'v0.5.8 Invoice Print Persistence: InvoiceWriteService, print templates, PrintLogWriteService — migration 0007',
                'v0.5.7 OpenCart Test Sync + Import: config/opencart.php, status mapping writes, controlled import max 50 — migration 0004',
                'v0.5.6 Post-Staging Module Foundations: return batch owner approve, Test Sync preview UI, packing slip preview, supplier quick invoice utility log + calculator — stock deduction and live sync still blocked',
                'v0.5.0 Staging Domain Launch Readiness: optional HTTP basic auth gate, docs/STAGING-DEPLOYMENT.md, staging env config',
                'v0.4.9.0 Supplier and Owner Dashboard Cleanup: role-based dashboards, supplier login (supplier/supplier), collapsed planning text',
                'v0.4.8.0 Supplier Statement Reports Foundation: /reports with 15 owner reports, print and CSV export',
                'v0.4.7.0 Supplier Payable Ledger Foundation: dispatch snapshot → payable draft, manual ledger entries, owner post/reject on /supplier-payables',
                'v0.4.6.0 Vendor Return Receive Foundation: /return-receive confirms Supplier Return (Hub/Courier + Customer to Supplier) and Lokkisona Owner Warehouse returns with reason, received confirmation, supplier condition, and structured notes in workflow history — receive confirmation only, no payable/ledger/stock/invoice/sync',
                'v0.4.5.0 Daily Dispatch Report + Cost Snapshot Foundation: /dispatch-reports selects shipped orders (max 50), creates DDMMYYYY or DDMMYYYY-Pn batch locked on create with immutable supplier cost snapshot, moves orders to dispatch_report_created with workflow history note, shows Created Report section on /order-workflow, blocks duplicate inclusion — no payable/stock/invoice/sync',
                'v0.4.4.0 Fulfillment Workflow Action Foundation: /order-workflow implements full IBS supplier status sequence with grouped orders, allowed-action buttons only, required notes for exceptions and manual dispatch gate, workflow history per order, and manual-order status mirror via order_reference — no dispatch/return/payable tables',
                'v0.4.4.0 Manual Order Create Gate Repair: /manual-orders write form uses manualOrderCreateForm gate (0005 order tables + Group B product/source tables) so DEV/TEST create form appears when all required tables are present',
                'v0.4.2.9 Manual Order Create QA and Audit Visibility Repair: /manual-orders restores the full DEV/TEST create form, requires dev/test confirmation and note, shows safety badges, displays latest manual order and item confirmation tables, and surfaces ERP bridge/read inventory visibility without payable, stock, invoice, or channel sync creation',
                'v0.4.2.8 Cost Stock History UI and Audit Confirmation Repair: Product Control passes cost/stock history read inventory, shows Audit Confirmation table with bold notes directly under the cost/stock form, and creates audit rows when same-value cost/stock is saved with a note',
                'v0.4.2.7 Cost/Stock Note Audit: cost/stock note is saved into visible history even when the entered value is the same as current value',
                'v0.4.2.6 Cost/Stock History Visibility: /product-control shows latest cost/stock history notes, old/new values, product/variant reference, and raw read inventory for ibs_product_cost_histories and ibs_product_stock_histories',
                'v0.4.2.5 Product Variant/Option UI: /product-control product dropdown, variant/option form with validation, variant inventory table, split write gates for product/variant/cost-stock',
                'v0.4.2.4 Migration Table Prefix Safety: database/migrations/*.sql draft table names use ibs_* prefix matching config/database.php; Dev DB Activation warns on non-prefixed table mismatch',
                'v0.4.2.3 Write Form Table-Gate UI: write/action forms hidden with warning until all required ibs_* tables exist; read inventory and planning content always visible',
                'v0.4.2.2 Dev DB Activation Helper: /dev-db-activation shows read-only table verification for Groups A–F, migration apply order, after-apply test flow, and blocked actions — no SQL execution',
                'v0.4.2.1 Sprint Merge QA: /database-safety shows sprint merge checklist, dev DB activation guide, and write module QA matrix — no live production activation yet',
                'v0.4.2 Dispatch Report Create: POST /dispatch-reports/create builds dispatch batch from ready_for_dispatch orders with dispatch_report_items and cost snapshots',
                'v0.4.1 Order Workflow Action: POST /order-workflow/action with state machine transitions and order_workflow_histories audit rows',
                'v0.4.0 Manual Order Create: POST /manual-orders/create with cost snapshot, duplicate external reference blocking, and bridge to ibs_orders',
                'v0.3.6 Launch Cutover Lock: POST /supplier-opening-balances/launch-lock sets launch_cutovers locked state',
                'v0.3.5 Opening Balance Create/Approve: draft opening balances with owner approval and payable ledger seeding on approve',
                'v0.3.4 Product Cost/Stock Change: cost and stock history tables written on each manual change',
                'v0.3.3 Product/Variant Create/Edit: controlled product and variant forms on /product-control',
                'v0.3.1–v0.3.2 Supplier/Business Source Create/Edit: first write services with CSRF and owner-approved whitelist paths',
                'v0.3.0 Manual Migration Apply Guide: activation checklist and apply order on /migration-files',
                'v0.2.9 Read Foundation QA Gate: full ReadOnlyRepositoryRegistry, model-pending inventory, write-path whitelist for checkpoint',
                'Admin, Activity, and Invoice Read Foundation: /invoice-printing, /activity-log, /users, and /roles-permissions wire InvoiceReadService, ActivityLogReadService, UserReadService, and RoleReadService with read inventory cards for ibs_invoices, ibs_activity_logs, ibs_users, and ibs_roles',
                'Completes read-only coverage for remaining core model contracts: Invoice, ActivityLog (DB contract only), User, and Role',
                'Activity log page keeps file-based App\\ActivityLog runtime logging unchanged while adding separate database read inventory from App\\Models\\ActivityLog',
                'User read inventory redacts sensitive fields such as password_hash in row display via shared read-inventory-card partial',
                'SELECT only on admin pages — no database writes, no user creation, no password change, no role/permission mutation, no invoice creation, no print log creation, no activity log DB write, and no migration apply',
                'Empty-state messages cite migrations 0002_core_users_roles_activity.sql and 0007_invoices_printing_supplier_tools.sql with ibs_ prefixed table names',
                'Dispatch, Return, and Payable Read Foundation: /dispatch-reports, /return-receive, and /supplier-payables wire DispatchReportReadService, ReturnReceiveReadService, and PayableLedgerReadService with read inventory cards for ibs_dispatch_reports, ibs_return_receives, and ibs_payable_ledgers',
                'New SELECT-only repositories and read services for DispatchReport, ReturnReceive, and PayableLedger following the existing BaseReadOnlyRepository pattern',
                'SELECT only on dispatch, return, and payable pages — no database writes, no dispatch batch creation, no return receive submit, no payable ledger creation, no supplier payment, no settlement action, and no migration apply',
                'Empty-state messages cite migration 0006_dispatch_returns_payables.sql and ibs_ prefixed table names when tables are not manually applied yet',
                'Order and Order Item Read Foundation: /order-workflow wires OrderReadService, OrderItemReadService, and OrderWorkflowHistoryReadService with read inventory cards for ibs_orders, ibs_order_items, and ibs_order_workflow_histories',
                'New SELECT-only repositories and read services for Order, OrderItem, and OrderWorkflowHistory following the existing BaseReadOnlyRepository pattern',
                'Order Workflow hybrid page keeps planning foundation content and adds Read-Only Order Workflow Inventory (v0.2.6) with try/catch fallbacks and up to 50 SELECT rows per table',
                'SELECT only on order workflow — no database writes, no order status action, no workflow mutation, no sync/import, and no migration apply from this page',
                'Empty-state messages cite migration 0005_orders_manual_orders_workflow.sql and ibs_ prefixed table names when tables are not manually applied yet',
                'Opening Balance and Launch Cutover Read Foundation: /supplier-opening-balances wires SupplierOpeningBalanceReadService and LaunchCutoverReadService with separate read inventory cards for ibs_supplier_opening_balances and ibs_launch_cutovers',
                'Supplier Opening Balances hybrid page keeps planning foundation content and adds Read-Only Opening Balance / Launch Cutover Inventory (v0.2.5) with try/catch fallbacks and up to 50 SELECT rows per table',
                'SELECT only on supplier opening balances — no database writes, no payable ledger creation, no launch lock action, and no migration apply from this page',
                'Empty-state messages cite migration 0008_supplier_opening_balances_launch_cutovers.sql and ibs_ prefixed table names when tables are not manually applied yet',
                'Product and Variant Read Foundation: /product-control wires ProductReadService and ProductVariantReadService with separate read inventory cards for ibs_products and ibs_product_variants',
                'Product Control hybrid page keeps planning foundation content and adds Read-Only Product Inventory (v0.2.4) with try/catch fallbacks and up to 50 SELECT rows per table',
                'Shared read-inventory-card partial now accepts a configurable card title for reuse across suppliers, business sources, and product control pages',
                'SELECT only on product control — no database writes, no sync, no stock change, no product cost change, and no migration apply from this page',
                'Supplier and Business Source Read Foundation: /suppliers and /business-sources wire SupplierReadService and BusinessSourceReadService with graceful empty states when MySQL or tables are unavailable',
                'Hybrid module pages keep planning foundation content and add a live Read-Only Inventory card showing connection status, table readiness, model contract columns, row count, and up to 50 SELECT rows',
                'Read inventory builders use try/catch around read service construction and never throw fatal errors when the database is down or migration 0003 is not applied yet',
                'Empty-state messages cite prefixed table names (ibs_*) and manual migration 0003_business_sources_suppliers_products.sql with no page-load schema repair',
                'SELECT only on supplier and business source pages — no database writes, no sync, and no migration apply; write services remain a future owner-approved build',
                'Database Service Layer Read-Only Foundation: app/Database helpers (Connection, TableName, QueryGuard, ReadOnlyQueryException) and evolved App\\Database facade for SELECT-only access',
                'Read-only repository layer under app/Repositories with BaseReadOnlyRepository, ReadOnlyRepositoryRegistry, and six repositories for suppliers, business sources, products, product variants, supplier opening balances, and launch cutovers',
                'Read-only service layer under app/Services/ReadOnly with thin delegation and no write methods',
                'QueryGuard enforces SELECT, SHOW, DESCRIBE, and EXPLAIN only and rejects mutation SQL keywords at repository runtime',
                'TableName resolves ibs_ prefixed physical table names from model logical table contracts',
                'Database Safety page shows read-only repository inventory, query guard status, and per-table existence probes with row counts when tables exist',
                'Health check includes Read-Only Query Guard active status',
                'All core module and admin planning pages now show live read-only inventory where model contracts exist, including Invoice Printing, Activity Log DB inventory, Users, and Roles/Permissions',
                'Checkpoint database safety scan extended to block runtime mutation SQL in PHP code',
                'No CRUD screens, no database writes, no migration apply, no OpenCart/WooCommerce connection, and no live planning-page data replacement in this version',
                'Core Model Layer and Database Contract Foundation: app/Models classes are pure metadata contracts (table, ordered columns, explicit primary key) aligned to the manual migration drafts',
                'All 16 existing model column contracts corrected to exactly match draft tables in database/migrations (users, roles, activity_logs, business_sources, suppliers, products, product_variants, orders, order_items, order_workflow_histories, dispatch_reports, return_receives, payable_ledgers, invoices, supplier_opening_balances, launch_cutovers)',
                'New metadata-only App\\Models\\BaseModel with read-only table(), columns(), and primaryKey() accessors and no PDO, query, save, find, create, mutation, or schema logic',
                'New read-only App\\Models\\ModelRegistry providing an in-memory table-to-model map with no filesystem scanning side effects and no database calls',
                'App\\Models\\ActivityLog documented as a future database contract only and kept separate from the current file-based runtime logger App\\ActivityLog',
                'Remaining migration draft tables without a class are recorded as model pending rather than mass-adding new model classes in this version',
                'Model layer performs no SQL execution, no migration apply, and no database writes',
                'Supplier Opening Balance and Launch Cutover planning foundation page and sidebar item',
                'Supplier opening balance permissions for supplier_opening_balances.view, supplier_opening_balances.manage, and supplier_opening_balances.approve',
                'Opening balance planning documents payable-to-supplier, advance-from-supplier, neutral-zero-start, cut-off date, supplier/source selection, reference notes, proof attachment planning, owner approval, audit logging, and adjustment safety',
                'Example opening payable for Iqbal & Brothers documents estimated 1,200,000 BDT as a launch starting balance, not normal order payable',
                'Launch Cutover checklist documents go-live date, cut-off date, old manual payable confirmation, supplier/source confirmation, proof support, owner approval, lock after launch, and separation from new ERP transactions',
                'Supplier Payables, Suppliers, Manual Orders, Dispatch Reports, Return Receive, Build Queue, Dashboard, README, tools, and database docs now describe old manual balance vs new ERP ledger boundaries',
                'Migration Execution Lock planning foundation page and sidebar item',
                'Migration execution lock permissions for migration_execution_lock.view and migration_execution_lock.manage',
                'Execution lock planning documents manual-only apply protection, wrong environment protection, dirty Git protection, failed dry-run protection, missing approval protection, backup missing protection, checksum mismatch protection, double-apply protection, emergency stop planning, and final lock state preview',
                'Planned execution lock and lock audit fields documented without SQL execution, lock record writes, or migration execution code',
                'Migration Runner, Approval, Dry Run, Files, Database Safety, Build Queue, Dashboard, README, and tools docs now describe the draft to dry-run to approval gate to execution lock workflow',
                'Migration Apply Approval Gate planning foundation page and sidebar item',
                'Migration approval and apply permissions for migration_approval.view, migration_approval.manage, migration_apply.view, and migration_apply.manage',
                'Approval gate planning documents dry-run pass, database backup confirmation, environment confirmation, owner/admin approval, Red Issues clear state, checksum confirmation, apply order review, rollback plan review, production warning, and final apply lock planning',
                'Planned migration approval, approval checklist, and approval audit fields documented without SQL execution or approval record writes',
                'Migration Runner, Dry Run, Files, Database Safety, and Build Queue pages now state that future migration apply requires the manual approval gate',
                'Migration Dry Run Validator planning foundation page and sidebar item',
                'Migration dry-run permissions for migration_dry_run.view and migration_dry_run.manage',
                'Dry-run planning documents migration file scanning, SQL safety scanning, apply-order validation, duplicate migration key checks, checksum planning, environment safety, backup reminder, owner approval, and Red Issues Summary behavior',
                'Planned dry-run result and issue fields documented without executing SQL or writing records',
                'Migration Runner, Migration Files, Database Safety, and Build Queue pages require dry-run before future migration apply planning',
                'Real Migration Files planning foundation page and sidebar item',
                'Migration files permissions for migration_files.view and migration_files.manage',
                'Manual-only draft SQL migration files added under database/migrations for core users, business sources, products, status mapping, sync, orders, dispatch, returns, payables, invoices, printing, and supplier tools',
                'Draft SQL files include owner approval, backup-first, do-not-auto-run, and no page-load execution warnings',
                'Migration Files page documents file purpose, draft-only warning, manual apply, backup-before-apply, dry-run/check-first, apply order, rollback planning, and Red Issues Summary behavior',
                'Checkpoint database safety scan allows schema draft statements only in database/migrations SQL files and documentation while runtime code remains blocked',
                'Build Queue and Semi-Automation planning foundation page and sidebar item',
                'Build queue permissions for build_queue.view, build_queue.manage, build_automation.view, and build_automation.manage',
                'Safe one-build-at-a-time workflow documented: read queue, apply one task or small safe batch, run checkpoint, then stop for owner approval',
                'Semi-automation levels documented for manual workflow, queue-suggested next task, and small 2-3 page planning batches',
                'Blocked automation actions documented: auto commit, auto push, migration apply, OpenCart/WooCommerce sync, order import, payable mutation, stock deduction, and invoice generation',
                'Checkpoint-first rule, Git sync rule, owner approval before commit/push, and Red Issues Summary stop rule documented',
                'Planned build queue, build run, and red issue fields documented without automatic table creation or queue records',
                'Database Safety and Migration Runner pages state that build automation must never trigger migration apply',
                'Real Database Migration Runner planning foundation page and sidebar item',
                'Migration runner permissions for migrations.view, migrations.manage, migration_runner.view, and migration_runner.manage',
                'Owner/admin migration planning visibility without staff or supplier migration management access',
                'Manual-only, dry-run/check-first, backup-before-apply, owner/admin confirmation, audit/log, rollback, production safety, and Red Issues Summary rules documented',
                'Planned migration groups documented for users, activity logs, business sources, suppliers, products, stock/cost histories, status mappings, sync/imports, orders, dispatch, payables, returns, invoices, and supplier tool audit',
                'Planned migration, migration run log, and rollback fields documented without automatic table creation or migration records',
                'Database Safety links to Migration Runner planning and reinforces no page-load schema changes',
                'Local checkpoint route smoke includes Migration Runner and uses plain text ALL GREEN or RED ISSUES SUMMARY footer labels',
                'Manual and External Order planning foundation page and permissions without order creation or sync writes',
                'Sonamoni WooCommerce reference entry and offline/manual order planning before direct WooCommerce sync',
                'Manual order product mapping, shared vendor stock, cost snapshot, duplicate reference, confirmation, and audit rules documented',
                'Planned manual order, item, and audit fields documented without automatic table creation',
                'Supplier Tools planning foundation page and permissions without generator, calculator, or supplier tool writes',
                'Supplier Quick Invoice Generator planned as independent one-time supplier output with admin audit/history',
                'Simple Calculator planned as standalone tool with no payable, settlement, product cost, courier, stock, order, or accounting impact',
                'Planned supplier quick invoice, item, and audit fields documented without automatic table creation',
                'ERP Invoice and Packing Print planning foundation page and permissions without invoice generation or print writes',
                'Lokkisona-style, Sonamoni-style, and manual ERP invoice template planning from ERP order snapshots',
                'Real Lokkisona invoice sample, current invoice extension, and PIT Order Manager documented as read-only references only',
                'Customer invoice, packing slip, dispatch batch report, supplier product summary, return batch print, and payable settlement print types planned',
                'Sync Preview and Import Safety planning foundation page and permissions without live source connection or sync writes',
                'Multi-source plan for Lokkisona/OpenCart, Sonamoni/WooCommerce, and manual/external reference orders',
                'Shared supplier stock, ERP invoice planning, preview-before-import, duplicate blocking, and return candidate separation',
                'Planned sync preview, preview item, and import approval fields with preview totals and table columns documented',
                'Status Mapping and Sync Planning foundation page and permissions without OpenCart connection or mapping writes',
                'Source status to IBS workflow, Supplier Return, Lokkisona Return, and courier mapping rules documented',
                'Independent IBS workflow after sync, skip Missing/status 0, unmapped status safety, and Test Sync preview rules',
                'Sync preview counts, performance/sync safety limits, and planned mapping/sync preview/sync log fields documented',
                'Return Receive & Review Batch planning foundation page and permissions, owner/admin controlled',
                'Supplier Return vs Lokkisona Return separation with pre-submit total/cost-snapshot confirmation',
                'Supplier Return may affect payable only after review; Lokkisona Return has no payable deduction',
                'Return batch planning, payable adjustment review, remark/note, and optional image proof planned',
                'Planned return receive, return batch, return item, and payable adjustment fields documented',
                'Supplier Payable & Settlement planning foundation page and permissions without payable writes',
                'Product Cost Payable from dispatch cost snapshot, based on product cost only, not selling price',
                'Supplier Invoice, Additional Payable, Return/Damage Deduction, Payment, and Advance wording planned',
                'Net Payable to Supplier formula, owner/admin approval/audit, and supplier self-request rules planned',
                'Planned payable ledger, supplier invoice, supplier payment, and deduction fields documented',
                'Dispatch Batch / Dispatch Report planning foundation page and permissions without dispatch writes',
                'Locked dispatch gate after Shipped with single-supplier batches and batch locking documented',
                'Batch reference format DDMMYYYY(COUNT_ORDER) and cost snapshot for Product Cost Payable planned',
                'Delivery Stop exception, future print/export/download output, and dispatch performance rules planned',
                'Planned dispatch report and dispatch report item fields documented without database writes',
                'Order Workflow planning foundation page and permissions without order sync or database writes',
                'Independent IBS workflow with main fulfillment stages and exception stages documented',
                'Allowed transition matrix, no move back to New Order, and dispatch report gate after Shipped',
                'Source/status mapping used only at import, IBS workflow stays independent after sync',
                'Cost snapshot for payable, action confirmation/activity log, and performance/safety rules planned',
                'Channel-neutral order workflow ready for other suppliers, sources, and manual/offline orders',
                'Product Control foundation page and permissions without database writes or OpenCart sync',
                'Current supplier context Iqbal & Brothers with channel-neutral product/cost/stock planning',
                'Read-only platform fields and supplier-editable model, cost, and stock rules documented',
                'Cost/stock history, low-stock warning, option image, and dispatch/payable cost snapshot rules',
                'Planned product and variant fields for future sync and supplier operations',
                'Business Source and Sales Channel foundation page and permissions without database writes',
                'Current primary source Lokkisona.com documented without hard-coding future channels',
                'Manual, offline, ecommerce, marketplace, wholesale, and other source types prepared',
                'Future order source links prepared for supplier workflow, dispatch, returns, and payable',
                'Supplier foundation page and permissions without database writes',
                'Primary supplier Iqbal & Brothers documented with channel-neutral architecture',
                'Planned supplier fields and clean supplier accounting wording',
                'User Management foundation page and permission',
                'Future database user planning without writes',
                'Local checkpoint runner for PHP lint, route smoke, and safety checks',
                'Manual migration foundation',
                'Database safety page and permission',
                'Planned future table inventory',
                'Session authentication foundation',
                'Owner, admin, staff, and supplier roles prepared',
                'Config-backed permission policy service',
                'Permission-aware route protection and sidebar visibility',
                'File-backed activity log foundation',
                'Simple router',
                'Admin layout',
                'PDO database connection',
                'Health monitoring with database and storage status',
                'Git-based deployment',
            ],
        ];
    }
}
