# v1.9.4 Navigation Release Structure Cleanup — Handoff

**Version:** 1.9.4  
**Release label:** Navigation Release Structure Cleanup  
**Date:** 2026-06-09

## Goal

Reorganize sidebar for operational release: daily Fulfillment/Finance modules visible, admin under Settings, foundation/dev under Future Modules. Improve Sync Settings post-save clarity. No route or business-logic deletion.

## Menu map

### Dashboard
| Label | Route |
|-------|-------|
| Dashboard | `/dashboard` |

### Fulfillment
| Label | Route |
|-------|-------|
| Order | `/order-workflow` |
| Manual Order | `/manual-orders` |
| Dispatched Total | `/dispatch-reports` |
| Return List | `/return-receive` |
| Payables | `/supplier-payables` |
| Product Control | `/product-control` |

### Finance
| Label | Route |
|-------|-------|
| Balance Sheet | `/reports?report=monthly_payable` |
| Payable & Settlement | `/settlements` |
| Opening Balance | `/supplier-opening-balances` |
| Supplier Account / Ledger | `/reports?report=supplier_ledger` |

### Reports (collapsed)
| Label | Route |
|-------|-------|
| Sales / Orders | `/reports?report=product_sales` |
| Supplier | `/reports?report=supplier_statement` |
| Returns | `/reports?report=hub_return` |
| Cost / Stock | `/reports?report=product_dispatch` |

### Settings (collapsed)
| Label | Route |
|-------|-------|
| Sync Settings | `/sync-api-settings` |
| Status Mapping | `/status-mapping` |
| Suppliers | `/suppliers` |
| Business Sources | `/business-sources` |
| Users | `/users` |
| Roles | `/roles-permissions` |
| Database Safety | `/database-safety` |

### Future Modules (collapsed, visually secondary)
Analytics, Supplier Tools, Calculator, Invoice Printing, Pre-acquisition (Soon), Activity Log, Health, Migration tools, Build Queue, Dev DB Activation (local/dev only).

## Sync Settings improvements

- Saved URL strip and masked token: `Token saved ••••••••{last4}`
- Connection status card: product/order API, dispatch bridge, last connection test, last product/order sync
- Buttons: Save API Settings, Test Connection, Refresh Products (returns to Sync Settings), Sync Log

## Files changed

- `app/Permission.php` — menu tiers, labels, groups
- `resources/views/partials/sidebar-nav.php` — layout
- `public/assets/css/app.css` — future tier + sync saved strip
- `app/Services/ReadOnly/SyncApiSettingsReadService.php` — status extensions
- `resources/views/sync-api-settings/index.php` — UX
- `app/Controllers/SyncApiSettingsController.php` — page title
- `app/Controllers/SyncPreviewController.php` — `redirect_to` for preview-products
- `config/app.php`, `app/Controllers/VersionController.php`

## Acceptance checklist

- [ ] Dashboard at top
- [ ] Fulfillment group with 6 items and correct labels
- [ ] Finance group with Balance Sheet, Payable & Settlement, Opening Balance, Supplier Ledger
- [ ] Reports tier collapsible with 4 shortcuts
- [ ] Settings tier with Sync Settings first
- [ ] Future Modules grouped; migration routes still work
- [ ] Active sidebar for `match_query` report links
- [ ] Sync Settings shows saved URL + masked token after save
- [ ] `tools/check-local.ps1` green
- [ ] Screenshots in `docs/screenshots/v194-*.png` reviewed before commit

## QA commands

```powershell
E:\xampp\php\php.exe -l app\Permission.php
E:\xampp\php\php.exe -l app\Services\ReadOnly\SyncApiSettingsReadService.php
E:\xampp\php\php.exe -l app\Controllers\SyncApiSettingsController.php
E:\xampp\php\php.exe -l app\Controllers\SyncPreviewController.php
powershell -ExecutionPolicy Bypass -File tools\check-local.ps1
```
