# IBS-LK Business Manager — v0.8.0 "Release Candidate Hardening" Handoff

Built directly on the owner's uploaded local repo (v0.7.3 with the owner's fixes
already applied) — NOT a reconstruction. This is the key change from prior rounds:
working on your actual tree means your fixes are preserved and the recurring
SupplierPayablesController corruption cannot be reintroduced.

config/app.php => version 0.8.0, release_label "Release Candidate Hardening".

This release is hardening only: stability, consistency, and end-to-end trust.
No new features, no new schema, no migrations, no Git operations.

---

## RECURRING BUG — permanent fix confirmation (Section 3)

app/Controllers/SupplierPayablesController.php was checked verbatim. The method
exists exactly ONCE at the end of the class, intact:

    private function netPayableFormula(): array
    {
        return [
            'summary' => '...',
            'points' => [...],
        ];
    }

Why it kept breaking before: previous deliverables were built from a separate
reconstructed copy and shipped as a full-repo zip, which overwrote your good
controller. The str_replace insertion of loadEligibleReturnBatchesForDeduction()
immediately above netPayableFormula() was where the signature got clipped.

This time: I edited your uploaded repo in place and did not touch
SupplierPayablesController at all. Verified by static lint:
  - netPayableFormula definitions found: 1
  - ReturnReceiveWriteRepository public find(): 1
  - All 31 controllers: braces/parens/brackets balanced (0 imbalance)
  - No duplicate methods anywhere in app/

---

## Priority 1 — Stability & consistency

A) Zero PHP syntax issues across app/ — every controller lint-clean (static check).
B) All 9 operations page subtitles now use
     v<?= e($appVersion) ?> — <?= e($appReleaseLabel ?? '') ?>
   (dispatch-reports, return-receive, supplier-payables, settlements, sync-preview,
    status-mapping, invoice-printing, reports, product-control). They previously had
    the version but not the release label.
C) All 69 routes verified to resolve to a real Controller@method — no route points
   at a missing method, so no blank/fatal pages from routing. (Live HTTP 200 still
   to be confirmed by you with the dev server — see checklist.)
D) settlements/index.php — the 4 form actions (prepare, approve, mark-paid, close)
   now use the url() helper instead of hardcoded "/settlements/...".

## Priority 2 — End-to-end workflow

The full owner chain is wired and each step degrades safely (WriteGate warning, never
a crash) when its tables aren't Ready:
  product-control → manual-orders/order-workflow create → advance to Shipped →
  dispatch-reports (locked snapshot) → supplier-payables (post dispatch draft → approve)
  → return-receive (confirm → create batch → owner approve) →
  supplier-payables (return deduction draft → approve) → settlements (prepare → approve
  → close) → reports → activity-log.
No dev banners on the main view of any of these pages; empty states use empty-state.

## Priority 3 — Supplier role sanity

config/permissions.php role_permissions['supplier'] is scoped to:
  dashboard.view, orders.view, order_workflow.view/manage, return_receive.view/manage,
  returns.view, supplier_tools.view, supplier_quick_invoice.manage, supplier_calculator.view
It does NOT include supplier_payables, settlements, sync, status_mapping, or migrations,
so the sidebar and route authorization already hide/deny them. The supplier dashboard
task cards link only to /order-workflow and /return-receive (both allowed). No payable/
settlement/migration text on supplier dashboard load.

## Priority 4 — Reports & settlements visual

  - reports: Print + Export CSV buttons present and obvious; report selector primary.
  - settlements: summary stat cards kept; empty state links to /supplier-payables.
  - supplier-payables: stat cards + ledger primary; Net Payable Formula stays collapsed.

---

## Files changed in v0.8.0

- config/app.php (version 0.8.0)
- app/Controllers/VersionController.php (feature bullet)
- resources/views/settlements/index.php (url() helper on 4 form actions + label subtitle)
- resources/views/dispatch-reports/index.php (label subtitle)
- resources/views/return-receive/index.php (label subtitle)
- resources/views/supplier-payables/index.php (label subtitle)
- resources/views/sync-preview/index.php (label subtitle)
- resources/views/status-mapping/index.php (label subtitle)
- resources/views/invoice-printing/index.php (label subtitle)
- resources/views/reports/index.php (label subtitle)
- resources/views/product-control/index.php (label subtitle)

Everything else is unchanged from your uploaded v0.7.3 (including the v0.7.x backend
and all gold-standard pages).

---

## Quality gates (all passed, static)

1. SupplierPayablesController.netPayableFormula intact — 1 definition, signature + return present
2. No duplicate find() in any class
3. No hardcoded v0.6./v0.7. in any page-description
4. All <details class="planning-collapsible"> balanced
5. No mojibake (Ã / â€ / â†) in any view or controller

---

## Owner browser test (run before committing)

  powershell -ExecutionPolicy Bypass -File tools\check-local.ps1   → expect [OK] ALL GREEN
  php -S 127.0.0.1:8010 -t public public/router.php

As admin/admin:
  [ ] Header badge shows v0.8.0
  [ ] /dashboard Quick Control links all open
  [ ] /supplier-payables loads (NOT blank/fatal) — stat cards + ledger visible
  [ ] /order-workflow cards show Total Qty, Cost, Source/OC Ref, OC Order Status
  [ ] /return-receive batch create + detail expand + owner approve
  [ ] /dispatch-reports create batch form works
  [ ] /settlements summary cards + prepare form (actions use url())
  [ ] /reports select report + Print/CSV
  [ ] every ops page subtitle reads "v0.8.0 — Release Candidate Hardening"

As supplier/supplier:
  [ ] /dashboard task cards only, no dev/migration text
  [ ] no sidebar links to payables/settlements/sync/migrations
  [ ] /order-workflow and /return-receive work

I cannot run PHP or the checkpoint here (no runtime/network in this environment), so
the above is validated statically. If check-local.ps1 reports RED, paste the summary
and I will fix and return a new zip.

## Still blocked for v1.0.0 (unchanged)

Stock deduction, live full sync, WooCommerce auto-sync, page-load DDL, auto-migration.
These remain off until you approve them as their own tasks.

No git operations performed. Commit when satisfied:
  git add -A && git commit -m "v0.8.0 Release Candidate Hardening"
